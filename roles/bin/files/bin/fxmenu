#!/bin/sh

# fxmenu float 500x10 fzf-launch

term="foot"
app_id="$1"
window_size="$2"
command="$3"
"$term" -w "$window_size" -a "$app_id" -e "$command" "$4" 2>/dev/null 1>&2
# "$term" -w "$window_size" -a "$app_id" -e "$command" "$4"
